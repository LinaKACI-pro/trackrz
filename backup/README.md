Python backend backup

This directory contains the previous Python backend and its tests.
It is not built, deployed or started by the current application.

Emergency run from the repository root:
  python3 backup/server.py

Legacy tests:
  python3 -m unittest backup/test_server.py
